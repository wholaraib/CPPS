#include <iostream>
#include <vector>
#include <map>
#include <set>
#include <cmath>
#include <algorithm>
#include <string>
#include <stack>
#include <queue>
using namespace std;

void print(vector<int> arr)
{
    for (auto i : arr)
        cout << i << " ";
    cout << endl;
}
int main()
{
    // vector <int> nums;
    // nums .push_back(4);
    // nums.push_back(7);
    // nums.push_back(8);
    // nums.push_back(2);
    // nums.push_back(3);
    // nums.push_back(1);
	// vector <int> ans;

	// ans.insert(ans.begin(),nums.begin(),nums.end());
	// print(ans);   
}